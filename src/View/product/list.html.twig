{% extends 'layout.html.twig' %}
{% block content %}
{% for product in products %}
<div class="col-md-4">
    <div class="card mb-4 shadow-sm">
        <div class="card-body">
            <p class="card-text">{{"name" ~" "~ product.name}}</p>
            <p class="card-text">{{"date de création" ~" "~product.DateCreation}}</p>
            <p class="card-text">{{" date d'expiration" ~" "~ product.DateExpiration}}</p>
            <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                    <a href="/products/editproduct?id={{ product.id }}" class="btn btn-sm btn-outline-secondary">Edit</a>
                    <a href="/products/deleteproduct?id={{ product.id }}" class="btn btn-sm btn-outline-secondary">delete</a>
                </div>
            </div>
        </div>
    </div>
{% endfor %}
{% endblock %}